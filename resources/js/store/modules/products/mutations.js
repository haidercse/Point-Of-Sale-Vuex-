export const UPDATE_PRODUCTS = (state, updateProducts) => {
    state.updateProducts = updateProducts;
}

export const SET_PRODUCTS = (state, products) => {
    state.allProductsWithSize = products;
}